/** v3 signal thresholds — single tuning file per upgrade spec §1. */
export const CFG = {
  VELOCITY_WINDOW_MS: 10 * 60 * 1000,
  VELOCITY_MIN_GAIN: 0.5,
  VELOCITY_COOLDOWN_MS: 30 * 60 * 1000,
  LIQ_DIVERGENCE_MIN_MULT: 2.0,
  LIQ_DIVERGENCE_DROP_PCT: 0.30,
  LIQ_DIVERGENCE_COOLDOWN_MS: 6 * 60 * 60 * 1000,
  RETEST_PEAK_MIN: 3.0,
  RETEST_BAND: [0.95, 1.10],
  LIFECYCLE: {
    COOKING_MIN: 1.5,
    SENDING_MIN: 3.0,
    BLEED_FROM_PEAK: 0.5,
    DEAD_MULT: 0.3,
    DEAD_AGE_H: 24,
  },
  CONFLUENCE_WINDOW_MS: 15 * 60 * 1000,
  CONFLUENCE_MIN_CHANNELS: 2,
  WALLET_CONFLUENCE_COOLDOWN_MS: 6 * 60 * 60 * 1000,
  WALLET_CONFLUENCE_MAX_AGE_MS: 7 * 24 * 60 * 60 * 1000,
  RUGSCAN_TIMEOUT_MS: 8000,
  POSITION_TIERS: [2, 3, 5, 10],
  RECAP_DAY_UTC: 0,
  MAX_TAGS: 3,
  CALLER_RUG_AGE_MS: 24 * 60 * 60 * 1000,
  RESOLVED_CALL_AGE_MS: 24 * 60 * 60 * 1000,
};
